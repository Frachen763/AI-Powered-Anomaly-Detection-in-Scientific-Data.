apiVersion: 1
datasources:
  - name: Supabase
    type: postgres
    url: ${PG_HOST}
    user: ${PG_USER}
    secureJsonData:
      password: ${PG_PASSWORD}
    database: ${PG_DB}
    jsonData:
      sslmode: "require"
